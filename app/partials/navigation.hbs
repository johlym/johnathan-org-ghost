<div class="navigation">
    {{#foreach navigation}}
    <div class="link link-{{slug}}{{#if current}} link-current{{/if}}"><a href="{{url absolute="true"}}">{{label}}</a></div>
    {{/foreach}}
</div>